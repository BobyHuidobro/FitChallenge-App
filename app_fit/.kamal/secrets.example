# Secrets file example for Kamal deployment
# Copy this file to .kamal/secrets and fill in your actual credentials
# DO NOT commit .kamal/secrets to git!

# Docker registry password (get from Docker Hub -> Account Settings -> Security -> Access Tokens)
KAMAL_REGISTRY_PASSWORD=your_docker_registry_password_here

# Rails master key (automatically read from config/master.key)
RAILS_MASTER_KEY=$(cat config/master.key)

# Database configuration
DB_HOST=localhost_or_db_server_ip
APP_FIT_DATABASE_PASSWORD=your_postgres_password_here
