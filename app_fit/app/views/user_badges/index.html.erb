<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-5" style="color: #2F855A;">🏆 Mis Badges</h1>
        <span class="badge rounded-pill fs-6" style="background-color: #38B2AC;">
            <%= @badges.count %> Badges
        </span>
    </div>

    <% if @badges.any? %>
        <div class="row">
            <% @badges.each do |badge| %>
                <div class="col-lg-3 col-md-4 col-sm-6 mb-4">
                    <div class="card h-100 shadow-sm">
                        <div class="card-body text-center">
                            <div class="mb-3">
                                <img src="<%= badge.icon_url %>" alt="<%= badge.name %>" style="width: 80px; height: 80px; object-fit: contain;">
                            </div>
                            
                            <h4 class="card-title">
                                <%= link_to badge.name, badge_path(badge), class: "text-decoration-none", style: "color: #2C7A7B;" %>
                            </h4>
                        </div>

                        <div class="card-footer bg-transparent border-0 text-center">
                            <%= link_to "Ver detalles", badge_path(badge), class: "btn btn-sm text-white", style: "background-color: #2F855A;" %>
                        </div>
                    </div>
                </div>
            <% end %>
        </div>
    <% else %>
        <div class="alert alert-info">
            Aún no has obtenido ningún badge. ¡Sigue participando en los desafíos para ganar badges!
        </div>
    <% end %>
</div>