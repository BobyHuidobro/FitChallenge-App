<div class="container mt-5">
        <div class="card mx-auto" style="max-width: 500px;">
            <div class="card-body d-flex justify-content-between align-items-center">
                <%= link_to "← Back to Badges", badges_path, class: "btn text-white", style: "background-color: #2F855A;" %>

                <div class="d-flex gap-2">
                    <% if can?(:edit, @badge) %>
                        <%= link_to "✏️", edit_badge_path(@badge), class: "btn btn-primary btn-sm" %>
                    <% end %>
                    <% if can?(:destroy, @badge) %>
                        <%= link_to "🗑️", badge_path(@badge),
                            data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this badge?" },
                            class: "btn btn-danger" %>
                    <% end %>
                </div>
            </div>
        </div>

    <div class="card mx-auto" style="max-width: 500px;">
        <div class="card-body text-center">

            <div class="mb-4 text-center">
                <img src="<%= @badge.icon_url %>" alt="<%= @badge.name %>" class="mb-3 d-block mx-auto" style="width: 120px; height: 120px;">
            </div>
            <h1 class="display-5 mb-3 fw-bold" style="color: #2F855A;"><%= @badge.name %></h1>
            <% if @badge.description.present? %>
                <p class="text-muted"><%= @badge.description %></p>
            <% else %>
                <p class="text-muted">No description avalaible.</p>
            <% end %>

        </div>
    </div>
    
</div>